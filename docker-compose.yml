services:
  ailift-dev:
    image: node:20-alpine
    working_dir: /app
    environment:
      PORT: 3000
    ports:
      - "3000:3000"
    volumes:
      - ./:/app
      - ailift_node_modules:/app/node_modules
    command: sh -c "npm install && npm run dev"

volumes:
  ailift_node_modules:
